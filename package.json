{
  "name": "tronsec",
  "private": true,
  "description": "TRONSEC — public app export (readable JS)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "check:js": "node tests/check-js-syntax.mjs",
    "check:guards": "node tests/check-export-guards.mjs"
  }
}
