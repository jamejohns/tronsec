# Export-only folder

This directory is the **public GitHub export** of the TRONSEC application.

- Updated by `python scripts/sync-github-export.py` from `staging/app/` in the private monorepo.
- **Do not** deploy this folder to tronsec.io — production uses `dist/`.
- Default branch on GitHub: **`main`**.
